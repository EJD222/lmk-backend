info:
  name: Get Session By Link ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}sessions/link/{{link_id}}"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8000/
    - name: link_id
      value: HIj9xEG6Gw

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
